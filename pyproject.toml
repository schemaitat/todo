[tool.uv.workspace]
members = ["api", "automation"]

[dependency-groups]
dev = [
    "ruff>=0.8",
    "ty>=0.0.1a6",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B", "SIM", "RUF"]
ignore = ["E501", "B008"]
