- run quality checks after each code change using the quality checker subagent
- implementing a ticket or feature follows git flow. use conventional commits and 
    squash merge. only merge with approval by the user. Once finished mark item as done.